# HANDOFF — sir_albert_os build (2026-07-31)

## Cold start (do in order)
1. `cd /Users/galta/Development/sir_albert && git checkout os/foundation`
2. Read `.memory-bank/sir_albert_os-BUILD.md` (the ledger — full per-task status + log).
3. `/reload-plugins` (or restart Claude Code) to load the new skills. Verify these appear: `/sir-albert:` **handoff, resume, sync, decide, discover, learnings, freeze, retro** (core) + **param-audit, gtm-gate, build-discipline, data-review, n8n-triage, hubspot-safety, discovery-lens** (packs/biz) + folded **ga4-regex, monday-data-viz-vibe, monday-presentation-v2, monday-brand-guidelines**.
4. Run the M6 smoke test (below).

## Current state
- Branch `os/foundation`, ~14 commits, **LOCAL only** — nothing pushed (gal-Tab ≠ dapulse/mondaycom org; push/PR needs your OK).
- **Built + verified (each task adversarially):** M0 (foundation + RESOLVER), M1 (handoff/resume/sync), M2 (identity + rules + PREAMBLE), M3 (6 pack skills + attester mechanism), M4.1–4.5 + 4.7 (decide/discover/learnings/freeze/retro + autonomy + 2 hook scripts), M5.0 (portfolio), M5.1a (allowlist JWTs removed), M5.2 (folded 4 loose skills + `discovery-lens` rename), M5.3-local (kb-query retired → llm-wiki-agent).
- **Global change made:** `~/.claude/CLAUDE.md` now `@`-imports `os/identity/USER.md` + `SOUL.md` (guard.sh block confirmed intact). **Revert = delete those 2 import lines.**
- Loose skills moved to `/tmp/sir_albert_os-loose-backup-20260731` (recoverable, incl. git history).

## Decisions + WHY
- **Home** = gal-Tab/sir_albert, local-only; dapulse mirror deferred to the 80x distribution tier (compliance).
- **M5.1b (mcp.json JWT) = SKIP** — not essential (token is a local file; the real exposure was the allowlist, fixed in 5.1a). Optional: rotate the bigbrain token if it was ever pushed/shared.
- **HEARTBEAT not @-imported** (consumed by hooks/cron) — keeps global context lean.
- Packs = 4 registered plugin categories; skills stay flat-namespaced `/sir-albert:<name>`.

## Gotchas / risks
- `plugin.json` is LIVE via the `~/.claude/skills/sir-albert` symlink — never break it. Empty listed skill dirs ARE tolerated (verified).
- New hooks do NOTHING until registered in `~/.claude/settings.json` (see Next Tasks A).

## Next tasks (deferred — need you or an external system)
### A. Activate hooks (live settings.json — you apply; snippets are in the skills)
- **/freeze:** add a PreToolUse `Edit|Write` hook → `hooks/freeze-guard.sh` (snippet in `skills/core/freeze/SKILL.md`).
- **/retro:** **APPEND** a SessionEnd hook → `hooks/session-record.sh` (snippet in `skills/core/retro/SKILL.md`). **Do NOT clobber** the existing token-optimizer/axcli SessionEnd entries.
- Optional: SessionStart `/resume` nudge + `learnings` auto-surface.
### B. Cross-repo (need your OK per security policy — external repos)
- **M5.2c:** reconcile `gtm_agent/skills/monday-mops-triage` vs sir_albert's authoritative copy — keep one home.
- **M5.3 consumers:** point `open_claw` + `Investment-agent` + the `llm-wiki-agent-dev` marketplace at `agent_knowledgebase` main; retire the stale v0.4.0.
- **M5.4:** dedup `cookbook`/`data-cookbook` clones; archive `marketing-cookbook_old`; handle empty `mopa_know`/`team-context`; install `usage-insights@agentic-builders-hub` (global) for the monthly spend check.
### C. External runtime
- **M4.6 dream cycle:** implement the HEARTBEAT weekly block (wiki-compile · canonicalize `.compound` · `stale_after` sweep · link freshness) as a **NanoClaw cron**; it proposes via PR, never publishes.
### D. Heavy / data
- **M4.5 retro backfill:** run once over ~3,853 historical prompts + `.compound/` → the initial trigger-fix PR.
- **M3.4 metrics:** pin real z2h looks (delivery funnel product→sGTM→GA4, MQL/attribution, tag-blocking) as canonical `<slug>.json` + `<slug>.md` (from `os/knowledge/metrics/_TEMPLATE.md`); the attester + `data-review` do the rest.
### E. TODO(gal) content to fill
- `os/identity/USER.md`: pronouns · key people · z2h + kremer channel IDs.
- `os/rules/gtm-naming.md`: the full tag/variable/trigger naming pattern.
- `os/rules/consent-map.md`: CookieHub source link + the real next-review date.
### F. Finish
- Wire `slack-in-my-voice` / `linkedin-in-my-voice` to read channels from `USER.md`.
- When distributing: mirror to a **dapulse-org** repo (I'll help with the migration).

## M6 smoke test (you run — needs live slash commands + real domains)
1. **GTM:** plan a small tag change → `/sir-albert:gtm-gate` fires (naming + consent) → dry-run apply → `/sir-albert:handoff`.
2. **Data:** `/prototype` a small view → query (Kremer/z2h) → `/sir-albert:data-review` → draft a leaders-channel Slack in voice (channel from USER.md).
3. **Build:** `/add-tool` on a sample process doc (via `build-discipline`).
4. **Continuity:** `/sir-albert:handoff` → `/clear` → new session → `/sir-albert:resume` → productive in ≤2 turns.
5. **Memory:** re-hit a `/sir-albert:decide` decision → auto-applied, not re-asked.

## Files to read first
`.memory-bank/sir_albert_os-BUILD.md` (ledger) · `os/RESOLVER.md` · `os/PREAMBLE.md` · `os/identity/*` · `REGISTRY.md`

## Paste-to-resume prompt
```
Resume the sir_albert_os build. Repo: /Users/galta/Development/sir_albert, branch os/foundation (LOCAL-only — don't push without my OK). Read .memory-bank/sir_albert_os-BUILD.md (ledger) and .memory-bank/HANDOFF-2026-07-31.md first. The core OS (M0–M4 + M5.0/5.1a/5.2/5.3-local) is built and verified. Remaining: activate the /freeze + /retro hooks in settings.json, then the deferred cross-repo items (M5.2c, M5.3 consumers, M5.4), the NanoClaw dream cycle (M4.6), the retro backfill (M4.5) and real z2h metric pinning (M3.4), and run the M6 smoke test. Ask me before any cross-repo change, live publish, or push.
```
