# Handoff — 2026-09-24

> **TLDR:** Rewired the sir_albert OS so the operating contract loads on **every** session (was firing ~10%) by inlining it into `~/.claude/CLAUDE.md`. Edits to `PREAMBLE.md` + `HEARTBEAT.md` are **done but uncommitted**; three decisions remain open — see Next tasks.

## Cold start (do in order)

1. `cd /Users/galta/Development/sir_albert`
2. `git checkout discovery-lens-voice-agents` (already the current branch)
3. Read: `~/.claude/CLAUDE.md` — the file we rewrote (the always-on contract; NOT in this repo, not version-controlled)
4. Read: this handoff doc
5. Run `/sir-albert:resume` to reload state in ≤5 bullets before doing anything.

## Current state

- **Branch:** `discovery-lens-voice-agents` — ahead of origin by 2 commits. **Mixes three unrelated concerns** (see Gotchas): discovery-lens (committed this morning, `f2eeb40`), the gal-va journal spec (`1836a41`), and this session's contract edits (uncommitted).
- **Done this session:**
  - Rewrote `~/.claude/CLAUDE.md`: added an **"Operating contract (always on)"** section — plan-first (defers to `superpowers:brainstorming`), anti-slop, use-the-skill, ask-before-live, **capture-corrections → MEMORY.md**, **subagents lean+model-routing** (Opus for planning/synthesis, Haiku for read/search/summarize), **minimal-impact**. Identity `@`-imports unchanged. **LIVE now, uncommitted, un-versioned.**
  - `os/identity/HEARTBEAT.md`: fixed the false "load PREAMBLE every message" claim → states CLAUDE.md is the always-on carrier; bumped `last_modified` to 2026-09-24. **Uncommitted.**
  - `os/PREAMBLE.md`: added a note marking CLAUDE.md as the always-on carrier so the two don't drift. **Uncommitted.**
  - Planned the **gal-va session-journal** feature (separate task): spec at `docs/specs/2026-09-22-session-summaries-to-gal-va.md` (committed `1836a41`), mirrored to **gal-va PR #19**, dev handoff at `.memory-bank/HANDOFF-2026-09-22.md`.
- **In-flight:** the 3 open decisions below. Nothing half-edited — the contract work is complete, just not committed or organized.
- **Blockers:** none.

## Decisions + WHY

- **Steal Boris's delivery mechanism, not his file.** Gal already has a richer OS than Boris's gist; the problem was it only loaded when a sir-albert skill fired (~10% of sessions, vs superpowers at 27%, 67% bare). Fix = inline the essentials into the always-on layer (`CLAUDE.md`), point to `os/rules/` for depth. Verified against 30 sessions.
- **Defer plan-first/debugging to superpowers** (don't duplicate) — Gal's choice; superpowers already owns those and works.
- **Capture → MEMORY.md as-is** (not a restructured store) — Gal's choice; the delivery mechanism was broken, not the store.
- **Deferred verify-before-done** — Gal's call; `data-review` skill covers the data case and it's more nuanced than a one-liner.
- **Added minimal-impact + subagents-liberally; skipped elegance** (redundant w/ SOUL) and the rest of Boris's rules (redundant or superpowers-owned).
- **gal-va journal ≠ MEMORY.md.** Journal = human/agent-readable session archive in `gal-va/notes/`, consumed by gal-va's OWN agent into its memory. MEMORY.md = behavioral rules auto-loaded into Gal's Claude Code context. Two stores, two jobs — do not merge.

## Gotchas / open risks

- **`~/.claude/CLAUDE.md` is NOT in a git repo** — no version history, no backup. The rewrite is live and un-tracked. Decide whether to track `~/.claude/` or symlink `CLAUDE.md` into sir_albert.
- **The review subagent was WRONG** that `output-style.md`/`autonomy.md` expired 2026-01-31 — actual `stale_after` is **2027-01-31** (valid). Do NOT re-bump them. (This is why the plan dropped from 4 edits to 3.)
- **Branch mixes 3 concerns** — committing the contract edits here muddies any discovery-lens PR. Recommend a fresh branch off `main`.
- Many `??` untracked files in `git status` are **pre-existing, not from this session** (create-skill, monday-presentation-v2, ga4-regex.zip, etc.) — don't sweep them into a commit.
- **gal-va PR #19 is open** and awaiting review/merge — the one live external artifact.

## Files to read first

- `~/.claude/CLAUDE.md` — the rewritten always-on contract (the core deliverable)
- `os/PREAMBLE.md`, `os/identity/HEARTBEAT.md` — the two uncommitted repo edits
- `docs/specs/2026-09-22-session-summaries-to-gal-va.md` — the gal-va journal spec (if picking up that thread)
- `.memory-bank/HANDOFF-2026-09-22.md` — the separate DEV handoff for implementing the gal-va feature

## Next task(s)

1. **Resolve the 3 open decisions:**
   - a) Version-control `~/.claude/CLAUDE.md`? (track `~/.claude/` in git, or symlink into sir_albert)
   - b) Where to commit `PREAMBLE.md` + `HEARTBEAT.md` — recommend a **fresh branch off `main`**, not `discovery-lens-voice-agents`.
   - c) Fold the 3 settled gal-va decisions into **PR #19**: cadence = **cron**, queue = **extra field on `sir-albert-sessions.jsonl`**, and note **gal-va's own agent consumes the journal into its memory**.
2. Once branched, commit the contract edits with a clear message and open a clean PR for the OS-contract change.

---

## Git state (auto-captured)

**Branch:** `discovery-lens-voice-agents`

**Status:**
```
## discovery-lens-voice-agents...origin/discovery-lens-voice-agents [ahead 2]
 M os/PREAMBLE.md
 M os/identity/HEARTBEAT.md
?? .memory-bank/HANDOFF-2026-09-22.md
?? .memory-bank/recon-inventory.md
?? docs/plans/2026-05-13-demo-dark-mode-toggle.html
?? docs/superpowers/plans/2026-09-09-create-skill.md
?? docs/superpowers/specs/2026-09-09-create-skill-design.md
?? hooks/hooks.json
?? hooks/validate-skill.py
?? skills/packs/gtm/ga4-regex.zip
```

**Recent commits:**
```
1836a41 docs: spec for session-summaries → gal-va/notes journal
47da5cf [kw-compound] Filed: Anomaly Detection — Failure-Shape-Specific Detectors
f2eeb40 feat(discovery-lens): split council voices into dispatchable agents/
a3c532b merge: create-skill meta-skill (agentskills-spec authoring loop)
76b9ec0 fix(create-skill): enforce per-scenario variance guard in threshold stop
50f0110 chore(create-skill): gitignore + untrack __pycache__ artifacts
f9fd1dd test(create-skill): dogfood T1 smoke verified
d150148 feat(create-skill): orchestration body
e9748c1 docs(create-skill): methodology, angles, convergence references
7541924 fix(create-skill): add inline angle definitions to author-discipline
```

**Diff stat vs merge-base:** 452 files, +30769/−216 — this is the **entire branch vs `main`** (includes create-skill, monday-presentation-v2, brand-guidelines, icon SVGs), NOT this session. This session touched only `os/PREAMBLE.md`, `os/identity/HEARTBEAT.md` (uncommitted) + `~/.claude/CLAUDE.md` (outside repo).

**Open PRs:** none in `sir_albert`. Separate: **gal-va PR #19** (session-journal spec mirror, awaiting review).
