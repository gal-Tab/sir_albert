# skills/core/ — domain-agnostic OS skills

Loaded everywhere, mode-independent. Invoked as `/sir-albert:<name>`.

Planned core skills:
- `handoff` — write the handoff doc **and** a paste-able restart prompt (M1.2)
- `resume` — reload the latest handoff, state the plan back in ≤5 bullets (M1.3)
- `sync` — post-merge git reconciliation, safe branch cleanup (M1.4)
- `decide` — record/apply decisions in `os/state/decisions.jsonl` (M4.1)
- `freeze` — scope Edit/Write to a path during investigation (M4.3)
- `discover` — front door that routes to the right thinking lens (M4.4)
- `retro` — self-improving skill loop from recent sessions (M4.5)

Every core skill boots from `os/PREAMBLE.md`.
